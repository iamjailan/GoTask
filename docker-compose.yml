services:
  api:
    build:
      context: .
    network_mode: host
    env_file:
      - .env
    environment:
      HTTP_ADDR: ":8080"
      MIGRATIONS_PATH: /app/migrations
      DATABASE_URL: ${DATABASE_URL:?Set DATABASE_URL in .env to the PostgreSQL URL}
